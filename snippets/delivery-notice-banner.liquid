{% comment %}
  Renders delivery notice banner

  Accepts:
  - title: {String} the title of the delivery notice banner (Optional)
  - svg_text: {String} the name of the svg from the assets directory to render the svg within the notice banner

  Usage:
  {% render 'delivery-notice-banner' %}
{% endcomment %}

<style>
  .delivery-notice__wrapper {
    color: var(--cstm-color-secondary);
  }

  .delivery-notice__content-wrapper {
    border: 1px solid var(--cstm-color-secondary);
    display: flex;
    align-items: center;
    border-radius: 0.6rem;
    overflow: hidden;
    background-color: var(--cstm-color-secondary);
  }

  .delivery-notice__icon-wrapper {
    padding: 1rem;
    height: 100%;
    display: flex;
    align-items: center;
  }

  .delivery-notice__content-wrapper svg {
    width: 2.4rem;
    height: auto;
    display: block;
    color: var(--cstm-color-white);
  }

  .delivery-notice__title p,
  .delivery-notice__title strong {
    color: inherit;
  }

  .delivery-notice__text {
    padding-block: 1rem;
    padding-inline: 1.6rem;
    width: 100%;
    background-color: var(--cstm-color-white);
  }

  .delivery-notice__text p,
  .delivery-notice__text strong {
    font-size: 1.4rem;
    color: inherit;
  }
</style>

<div class="delivery-notice__wrapper">
  <div class="delivery-notice__content-wrapper">
    <div class="delivery-notice__icon-wrapper">
      {% if svg_text %}
        {{ svg_text | append: '.svg' | inline_asset_content }}
      {% else %}
        {{ 'icon-delivery-box.svg' | inline_asset_content }}
      {% endif %}
    </div>
    <div class="delivery-notice__text">{{ title }}</div>
  </div>
</div>
