{% comment %}
  Renders notice banner

  Accepts:
  - id: {String} the identifier
  - title: {String} the title of the notice banner (Optional)
  - text: {String} the text of the notice banner
  - svg_text: {String} the name of the svg from the assets directory to render the svg within the notice banner
  - color: {String} the color of the notice banner (Optional)

  Usage:
  {% render 'notice-banner' %}
{% endcomment %}

<style>
  .block-{{ id }} {
    color: {{ color }};
  }

  .block-{{ id }} .notice__content-wrapper {
    border: 1px solid {{ color }};
    display: flex;
    align-items: center;
    border-radius: 0.6rem;
    overflow: hidden;
    background-color: {{ color }};
  }

  .notice__icon-wrapper {
    padding: 1rem;
    height: 100%;
    display: flex;
    align-items: center;
  }

  .notice__content-wrapper svg {
    width: 2.4rem;
    height: auto;
    display: block;
    color: var(--cstm-color-white);
  }

  .notice__text-wrapper {
    padding-block: 1rem;
    padding-inline: 1.6rem;
    width: 100%;
    background-color: var(--cstm-color-white);
  }

  .notice__title {
    color: inherit;
    font-size: 1.4rem;
    font-weight: 500;
  }

  .notice__text p,
  .notice__text strong {
    font-size: 1.4rem;
    color: inherit;
  }
</style>

<div class="block-{{ id }}">
  <div class="notice__content-wrapper">
    <div class="notice__icon-wrapper">
      {% if svg_text != blank %}
        {{ svg_text | append: '.svg' | inline_asset_content }}
      {% else %}
        {{ 'icon-delivery-box.svg' | inline_asset_content }}
      {% endif %}
    </div>
    <div class="notice__text-wrapper">
      {% if text != blank %}
        <div class="notice__title">{{ title }}</div>
      {% endif %}
      <div class="notice__text">{{ text }}</div>
    </div>
  </div>
</div>
